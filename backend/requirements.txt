fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
httpx==0.28.1
pyyaml==6.0.2
redis==5.2.1
python-dotenv==1.0.1

# Optional — local NLI. Without these the pipeline falls back to a
# deterministic lexical stub so tests and the eval harness still run.
# transformers==4.48.0
# torch==2.5.1
# sentencepiece==0.2.0

# Dev
pytest==8.3.4
pytest-asyncio==0.25.2
